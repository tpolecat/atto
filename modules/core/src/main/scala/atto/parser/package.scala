package atto

package object parser {
  
  object character extends Character
  object combinator extends Combinator
  object numeric extends Numeric
  object text extends Text

  object all extends Parsers

}